info:
  name: without bearer
  type: http
  seq: 3

http:
  method: GET
  url: http://127.0.0.1:5000/api/v1/inventory/{{inventory_item_id}}
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "401"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
