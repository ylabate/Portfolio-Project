info:
  name: Get Cart
  type: http
  seq: 10

http:
  method: GET
  url: http://localhost:5000/api/v1/cart
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
