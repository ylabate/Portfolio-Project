info:
  name: register
  type: http
  seq: 4

http:
  method: POST
  url: http://127.0.0.1:5000/api/v1/auth/register
  body:
    type: json
    data: |-
      {
          "username": "theo2",
          "email": "theo2@test.com",
          "password": "password123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
