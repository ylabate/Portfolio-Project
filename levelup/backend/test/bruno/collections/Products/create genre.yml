info:
  name: create genre
  type: http
  seq: 8

http:
  method: POST
  url: http://127.0.0.1:5000/api/v1/genres
  body:
    type: json
    data: '{ "name": "Simulation" }'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
