info:
  name: Add to Cart
  type: http
  seq: 11

http:
  method: POST
  url: http://localhost:5000/api/v1/cart/items
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
