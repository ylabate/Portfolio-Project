info:
  name: login
  type: http
  seq: 3

http:
  method: POST
  url: 127.0.0.1:5000/api/v1/auth/login
  body:
    type: json
    data: |-
      {
        "email": "test1user@example.com",
        "password": "azerty2115"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
