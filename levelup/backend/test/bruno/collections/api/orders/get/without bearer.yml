info:
  name: without bearer
  type: http
  seq: 1

http:
  method: GET
  url: http://127.0.0.1:5000/api/v1/orders
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "401"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
